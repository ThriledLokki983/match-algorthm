# Matching algorithm for two profiles based on the profile similarity metric 

class Match:
    def __init__(self, profile_one, profile_two) -> None:
        self.profile_one = profile_one
        self.profile_two = profile_two
        self.percentage = 0.0
        self.match_list = []
        
    def match(self) -> None:
        pass
    
    def check_match(self) -> None:
        pass
        
        
        